# ルールの適用について

このリポジトリのルールは `.claude/rules/` に置いています。
Claude Code / Codex が自動で読み込まない場合は、以下のいずれかで適用してください。

## 適用方法
1. `~/.claude/rules/` にコピーまたはシンボリックリンクを作成する
2. プロジェクト設定に明示し、常に参照できるようにする

## 目的
- リソース探索時に `resources/list` と `resources/templates/list` を優先利用する
- URI提示とリソース参照の流れを統一する
